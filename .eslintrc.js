module.exports = {
  root: true,
  extends: "@react-native",
  // Add any custom rules here if needed
  rules: {
    // Example: disable a specific rule if needed
    // 'no-console': 'warn',
  },
};
